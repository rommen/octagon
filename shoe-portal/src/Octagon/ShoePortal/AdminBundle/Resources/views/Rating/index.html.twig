{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Rating list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Value</th>
                <th>Idrating</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_rating_show', { 'id': entity.idRating }) }}">{{ entity.value }}</a></td>
                <td>{{ entity.idRating }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_rating_show', { 'id': entity.idRating }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_rating_edit', { 'id': entity.idRating }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_rating_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
