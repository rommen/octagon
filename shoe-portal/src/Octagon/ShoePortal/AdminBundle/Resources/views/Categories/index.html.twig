{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Categories list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Name</th>
                <th>Level</th>
                <th>Idcategories</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_categories_show', { 'id': entity.idCategories }) }}">{{ entity.name }}</a></td>
                <td>{{ entity.level }}</td>
                <td>{{ entity.idCategories }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_categories_show', { 'id': entity.idCategories }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_categories_edit', { 'id': entity.idCategories }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_categories_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
