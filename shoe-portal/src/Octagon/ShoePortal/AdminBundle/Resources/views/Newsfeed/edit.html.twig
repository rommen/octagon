{% extends "AdminBundle:Default:index_admin.html.twig" %}
{% block title %}Edit News feed{% endblock%}
{% block content %}
    <script>
        navigation('nav_newsfeeds');
    </script> 
    <div class="btn-group" role="group">
        <button type="button" class="btn btn-primary" onclick="pushButtonInnerHref(this);"> 
            <a href="{{ path('admin_newsfeed') }}">
                Back to the list
            </a>
        </button>           
        <div>{{ form(delete_form) }}</div>
    </div>
    {{ form(edit_form) }}
{% endblock %}
