{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Shoe creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_shoe') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
